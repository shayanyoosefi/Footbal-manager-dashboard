services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: academy
      POSTGRES_PASSWORD: academy
      POSTGRES_DB: academy_hub
    ports:
      - "5432:5432"
    volumes:
      - academy_pg_data:/var/lib/postgresql/data

volumes:
  academy_pg_data:
